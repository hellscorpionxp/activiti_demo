server:
  port: 8080
spring:  mvc:    view:
      prefix: classpath:/templates/      suffix: .html  datasource:    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/activiti?characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Shanghai
    username: root
    password: root  activiti:    db-history-used: true
    history-level: full
    check-process-definitions: true